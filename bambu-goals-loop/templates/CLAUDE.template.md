# {{PROJECT}} — Project Conventions

> **`AGENTS.md` is the single source of truth for conventions.**
> This file intentionally contains no conventions — do not add them here.
> Read `AGENTS.md` in full before writing code.

## Goal-based workflow (IMPORTANT)

1. Read `GOALS.md` (active goal) and `MEMORY.md` (state, decisions, blockers) on start.
2. Work only the active goal; do not pull future goals forward.
3. On session close: check boxes in `GOALS.md`, update `MEMORY.md`, leave the build green.
